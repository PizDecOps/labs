# 🚀 Практическая работа: Основы Bash

1. **Lab 1 (Навигация):** Найди скрытый файл в `~/bash/lab_1`, удали все `.jpg` и переименуй `report_v2.txt` в `final_report.txt`.
2. **Lab 2 (Фильтрация):** В `access.log` найди ошибки 404, выведи IP-адреса для ошибок 403 через `awk` и посчитай количество строк с IP `192.168.1.10`.
3. **Lab 3 (Потоки):** Сохрани первые 10 строк `service.log` в `start.txt` и выведи последние 5 строк в консоль.
